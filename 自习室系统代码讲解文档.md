# 自习室预约管理系统 —— 代码讲解文档

> 适用场景：期末课堂答辩 · 代码走读  
> 项目名称：`study-room-admin`  
> 技术栈：Vue 3 + TypeScript + Pinia + Vue Router + Element Plus + Axios + json-server

---

## 目录

1. [项目整体介绍](#一项目整体介绍)
2. [技术架构图](#二技术架构图)
3. [目录结构说明](#三目录结构说明)
4. [主要功能详解](#四主要功能详解)
   - 4.1 [预约座位功能（BookingView）](#41-预约座位功能bookingview)
   - 4.2 [后台管理功能（AdminView）](#42-后台管理功能adminview)
   - 4.3 [数据状态管理（useBookingStore）](#43-数据状态管理usebookingstore)
   - 4.4 [前后端通信层（API + axiosInstance）](#44-前后端通信层api--axiosinstance)
5. [次要功能简述](#五次要功能简述)
6. [数据流向总结](#六数据流向总结)
7. [答辩常见问题参考](#七答辩常见问题参考)

---

## 一、项目整体介绍

本系统是一个**自习室座位预约与管理平台**，分为两个主要角色：

| 角色 | 功能 |
|------|------|
| **普通用户** | 查看座位状态、选座、填写预约信息、取消预约 |
| **管理员** | 审批预约、管理座位、处理违规记录、发布系统通知 |

系统采用**前后端分离**架构：
- **前端**：Vue 3 单页应用（SPA），运行在 `http://localhost:5173`
- **后端**：`json-server` 轻量级 REST API 服务，监听 `http://localhost:3000`，数据持久化到本地 `db.json` 文件

启动方式（一键启动前后端）：
```bash
npm run dev:all
```

---

## 二、技术架构图

```
┌─────────────────────────────────────────────────────┐
│                    浏览器（用户操作）                    │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│                  Vue 3 前端应用                        │
│                                                       │
│  ┌──────────┐   ┌──────────┐   ┌─────────────────┐  │
│  │ HomeView │   │BookingView│  │   AdminView      │  │
│  │ 首页仪表盘 │   │ 预约座位  │   │ 后台管理控制面板  │  │
│  └──────────┘   └──────────┘   └─────────────────┘  │
│                        │                              │
│              ┌─────────▼──────────┐                  │
│              │  useBookingStore   │  ← Pinia 状态管理  │
│              │  (全局数据中心)     │                   │
│              └─────────┬──────────┘                  │
│                        │                              │
│              ┌─────────▼──────────┐                  │
│              │   API 层 (axios)   │  ← HTTP 请求封装   │
│              └─────────┬──────────┘                  │
└────────────────────────┼────────────────────────────┘
                         │ HTTP (REST API)
┌────────────────────────▼────────────────────────────┐
│           json-server（后端 REST API）                 │
│           监听端口：3000                               │
│  /bookings  /seats  /violations  /notices            │
│                        │                              │
│               ┌────────▼────────┐                    │
│               │    db.json      │  ← 数据持久化文件    │
│               └─────────────────┘                    │
└─────────────────────────────────────────────────────┘
```

---

## 三、目录结构说明

```
src/
├── api/                    # 所有 HTTP 请求封装
│   ├── axiosInstance.ts    # axios 实例（请求/响应拦截器）
│   ├── booking.ts          # 预约相关 API
│   ├── seat.ts             # 座位相关 API
│   ├── violation.ts        # 违规相关 API
│   ├── notice.ts           # 通知相关 API
│   ├── auth.ts             # 认证相关（预留）
│   └── index.ts            # 统一导出
│
├── stores/
│   └── useBookingStore.ts  # Pinia 全局状态管理（核心数据中心）
│
├── views/                  # 页面视图
│   ├── HomeView.vue        # 首页（仪表盘）
│   ├── BookingView.vue     # 预约页（核心用户功能）
│   └── AdminView.vue       # 管理员控制面板（核心管理功能）
│
├── components/             # 公共组件
│   ├── Base/
│   │   ├── BaseDialog.vue  # 通用对话框
│   │   └── BaseTable.vue   # 通用表格
│
├── router/
│   └── index.ts            # 路由配置（3 个路由）

├── types/                  # TypeScript 类型定义
│   ├── booking.ts          # 预约类型
│   ├── seat.ts             # 座位类型
│   ├── violation.ts        # 违规类型
│   ├── notice.ts           # 通知类型
│   └── user.ts             # 用户类型
│
├── utils/                  # 工具函数
│   ├── storage.ts          # localStorage 封装
│   ├── format.ts           # 时间格式化
│   ├── validate.ts         # 表单验证
│   └── message.ts          # 消息提示封装
│
├── App.vue                 # 根组件（包含导航栏）
└── main.ts                 # 应用入口
```

---

## 四、主要功能详解

---

### 4.1 预约座位功能（BookingView）

**文件路径**：`src/views/BookingView.vue`

这是用户最核心的操作页面，完成**"选座 → 填表 → 提交 → 取消"**的完整预约闭环。

#### 页面布局

页面分为左右两栏：

```
┌─────────────────────┬──────────────────────┐
│     左侧：座位选择区  │   右侧：预约信息表单    │
│                     │                      │
│  [区域筛选按钮组]    │  座位号（只读）         │
│  ┌────┐ ┌────┐      │  预约人姓名            │
│  │ A1 │ │ A2 │      │  学号/工号             │
│  └────┘ └────┘      │  联系电话              │
│  ┌────┐ ┌────┐      │  预约日期              │
│  │ B1 │ │ B2 │      │  时间范围（开始/结束）   │
│  └────┘ └────┘      │  备注（可选）           │
│  （颜色区分状态）     │  [提交] [重置] [返回]   │
│                     ├──────────────────────┤
│                     │   我的预约记录（底部）   │
└─────────────────────┴──────────────────────┘
```

#### 座位选择逻辑

```typescript
// 根据区域筛选座位（computed 计算属性，响应式自动更新）
const filteredSeats = computed(() => {
  const allSeats = bookingStore.seats || []
  if (selectedArea.value === 'all') return allSeats
  return allSeats.filter(seat => seat.area === selectedArea.value)
})

// 点击座位时触发
const selectSeat = (seat: any) => {
  // 只有 idle（空闲）或 available 状态的座位才能选中
  if (seat.status !== 'idle' && seat.status !== 'available') {
    ElMessage.warning('该座位不可用')
    return
  }
  selectedSeat.value = seat
  bookingForm.seatNumber = seat.number  // 同步填入表单
}
```


#### 表单验证规则

提交前必须通过以下所有验证：

```typescript
const bookingRules = {
  seatNumber: [{ required: true, message: '请选择座位' }],
  userName:   [{ min: 2, max: 20, message: '姓名 2-20 个字符' }],
  userId:     [{ pattern: /^[0-9A-Za-z]{4,20}$/, message: '4-20 位数字或字母' }],
  phone:      [{ pattern: /^1[3-9]\d{9}$/, message: '正确的手机号格式' }],
  date:       [{ required: true, message: '请选择预约日期' }],
  startTime:  [{ required: true, message: '请选择开始时间' }],
  endTime:    [{ required: true, message: '请选择结束时间' }]
}
```

另外有独立的时间段验证函数：

```typescript
const validateBookingTime = () => {
  // 结束时间必须晚于开始时间
  if (end <= start) { ElMessage.error('结束时间必须晚于开始时间'); return false }
  // 至少预约 1 小时
  if (end - start < 100) { ElMessage.error('预约时长至少1小时'); return false }
  // 最多预约 6 小时
  if (end - start > 600) { ElMessage.error('单次预约最多6小时'); return false }
  return true
}
```

#### 提交预约（核心流程）

```typescript
const submitBooking = async () => {
  // 第一步：校验表单（Element Plus 内置验证）
  await bookingFormRef.value?.validate()
  
  // 第二步：校验预约时间段
  if (!validateBookingTime()) return

  // 第三步：调用 Store 方法，向后端 POST /bookings 写入预约数据（持久化到 db.json）
  const newBooking = await bookingStore.addBooking({
    seatNumber, area, userName, userId, phone,
    date, startTime, endTime, note,
    status: 'pending'  // 初始状态为"待确认"
  })

  // 第四步：同步更新座位状态为"占用"（PATCH /seats/:id）
  await bookingStore.updateSeat(selectedSeat.value.id, {
    status: 'occupied',
    currentUser: bookingForm.userName
  })

  // 第五步：弹出成功确认对话框，重置表单
  showSuccessDialog.value = true
  resetForm()
}
```

**关键设计点**：提交预约和更新座位状态是**两步原子操作**，确保数据一致性——预约成功后座位立即显示为"占用"，其他用户刷新后看到的是最新状态。

#### 取消预约

```typescript
const cancelBooking = async (id: number) => {
  // 弹窗让用户二次确认
  await ElMessageBox.confirm('确定要取消这个预约吗？', '提示', { type: 'warning' })
  
  // 更新预约状态为 cancelled
  await bookingStore.updateBooking(id, { status: 'cancelled' })
  
  // 同时释放对应座位（恢复为 idle 状态）
  if (seat && seat.status === 'occupied') {
    await bookingStore.updateSeat(seat.id, { status: 'idle', currentUser: '' })
  }
}
```

---

### 4.2 后台管理功能（AdminView）

**文件路径**：`src/views/AdminView.vue`

这是管理员的控制面板，通过顶部 Tab 菜单切换四个子模块：

```
[ 预约管理 ] [ 座位管理 ] [ 违规处理 ] [ 系统通知 ]
```

#### 模块一：预约管理

**功能**：查看所有预约、审批/拒绝/取消预约，支持批量操作和筛选。

**筛选功能**（多条件组合）：

```typescript
const filteredBookings = computed(() => {
  let list = bookingStore.bookings || []

  // 关键字搜索（座位号 / 预约人 / 学号）
  if (bookingFilters.search) {
    list = list.filter(b =>
      b.seatNumber.includes(s) || b.userName.includes(s) || b.userId.includes(s)
    )
  }
  // 按状态筛选
  if (bookingFilters.status) {
    list = list.filter(b => b.status === bookingFilters.status)
  }
  // 按日期范围筛选
  if (bookingFilters.dateRange.length === 2) {
    list = list.filter(b => new Date(b.date) >= start && new Date(b.date) <= end)
  }

  // 前端分页（截取当前页数据）
  bookingPagination.total = list.length
  return list.slice(from, from + pageSize)
})
```

**批量操作**（通过、拒绝、取消）：

```typescript
const handleApproveMultiple = async () => {
  // 批量 PATCH —— json-server 不支持批量接口，Store 内部逐条发请求
  const ids = selectedBookings.value.map(b => b.id)
  await bookingStore.batchUpdateStatus(ids, 'confirmed')
}
```

**预约状态流转**：


#### 模块二：座位管理

**功能**：查看座位统计、新增/编辑/删除座位、切换维修状态。

**统计卡片**（实时计算）：

```typescript
// 这些都是 Pinia Store 中的计算属性，实时响应数据变化
const availableSeatsCount = computed(() => bookingStore.availableSeatsCount)  // 可用数量
const occupiedSeatsCount   = computed(() => bookingStore.occupiedSeatsCount)  // 占用数量
const maintenanceSeatsCount = computed(() => bookingStore.maintenanceSeatsCount) // 维修数量
```

**新增/编辑座位表单字段**：



#### 模块三：违规处理

**功能**：记录用户违规行为，标记处理状态，删除记录。

**违规类型**：超时占用、噪音干扰、物品占座、设备损坏、其他。

**违规处理流程**：

```typescript
const handleProcessViolation = async (id: number) => {
  // 调用 Store.processViolation → PATCH /violations/:id
  // 更新 status 为 'processed'，同时记录处理人和处理时间
  await bookingStore.processViolation(id, 'admin')
}
```

#### 模块四：系统通知

**功能**：发布/激活/过期/删除系统通知，支持通知分类和优先级。

**通知优先级**（通过左边框颜色区分）：

```css
.notice-card.priority-high   { border-left: 4px solid #f56c6c; }  /* 红色 = 高优先级 */
.notice-card.priority-medium { border-left: 4px solid #e6a23c; }  /* 橙色 = 中优先级 */
.notice-card.priority-low    { border-left: 4px solid #409eff; }  /* 蓝色 = 低优先级 */
```

**通知类型**：系统通知（info）、维护通知（warning）、活动通知（success）、紧急通知（danger）。

---

### 4.3 数据状态管理（useBookingStore）

**文件路径**：`src/stores/useBookingStore.ts`

这是整个应用的**数据中心**，基于 Pinia 实现，统一管理四类数据：

```typescript
export const useBookingStore = defineStore('booking', () => {
  // 四类核心数据（响应式 ref，任何组件读取都能自动更新 UI）
  const seats      = ref<Seat[]>([])       // 座位列表
  const bookings   = ref<Booking[]>([])    // 预约列表
  const violations = ref<Violation[]>([])  // 违规列表
  const notices    = ref<Notice[]>([])     // 通知列表

  // 加载状态（控制 UI 显示 loading 动画）
  const loading = ref({ seats, bookings, violations, notices })
})
```

**Store 的作用**：

1. **避免重复请求**：数据缓存在 Store 中，多个组件共享同一份数据
2. **统一数据更新**：修改操作先调用 API，成功后更新本地数组，UI 自动刷新
3. **计算属性**：自动派生统计数据（可用/占用/维修数量等），无需手动计算

**本地数据同步模式**（以 updateSeatData 为例）：

```typescript
const updateSeatData = async (id: number, data: Partial<Seat>) => {
  // 1. 先向后端发送 PATCH 请求（保证数据持久化）
  const res = await updateSeat(id, data)
  
  // 2. 成功后更新本地 seats 数组（UI 立即响应，不需要重新请求全量数据）
  const index = seats.value.findIndex(s => s.id === id)
  if (index !== -1) {
    seats.value[index] = { ...seats.value[index], ...res }
  }
  
  // 3. 重新获取统计数据
  await fetchSeatStats()
}
```

**定义的 TypeScript 接口**（数据结构规范）：

```typescript
// 座位
interface Seat {
  id: number; number: string; area: string
  status: 'idle' | 'occupied' | 'maintenance'
  features?: string[]; currentUser?: string
}

// 预约
interface Booking {
  id: number; seatNumber: string; userName: string
  userId: string; phone: string; date: string
  startTime: string; endTime: string
  status: 'pending' | 'confirmed' | 'cancelled' | 'completed'
  createdTime: string; note?: string
}
```

---

### 4.4 前后端通信层（API + axiosInstance）

**文件路径**：`src/api/axiosInstance.ts` 及 `src/api/*.ts`

#### axios 实例配置

```typescript
const axiosInstance = axios.create({
  baseURL: 'http://localhost:3000',  // json-server 地址
  timeout: 10000,                    // 超时 10 秒
  headers: { 'Content-Type': 'application/json' }
})
```

#### 请求拦截器（自动附加 Token）

```typescript
axiosInstance.interceptors.request.use((config) => {
  const token = getLocalStorage('token', '')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`  // 每个请求自动带上 token
  }
  return config
})
```

#### 响应拦截器（统一错误处理）

```typescript
axiosInstance.interceptors.response.use(
  (response) => response.data,   // 成功：直接返回 data，省去每次写 res.data
  (error) => {
    switch (error.response?.status) {
      case 401: ElMessage.error('登录已过期'); break
      case 403: ElMessage.error('没有权限'); break
      // 404 静默处理（json-server 数据不存在）
      default:  ElMessage.error('请求失败')
    }
    return Promise.reject(error)
  }
)
```

#### API 接口设计（以预约为例）

| 方法 | 接口 | 说明 |
|------|------|------|
| `GET` | `/bookings` | 获取所有预约（支持 status 过滤） |
| `GET` | `/bookings/:id` | 获取单条预约 |
| `POST` | `/bookings` | 创建预约（自动添加 createdTime） |
| `PATCH` | `/bookings/:id` | 更新预约字段（状态变更、审批等） |
| `DELETE` | `/bookings/:id` | 删除预约 |

批量更新没有专用接口，通过并发多个 PATCH 请求实现：

```typescript
export const batchUpdateBookingStatus = async (ids: number[], status: string) => {
  // Promise.all 并发发送多个请求，等所有都完成
  const promises = ids.map(id =>
    axiosInstance.patch(`/bookings/${id}`, { status, updatedTime: new Date().toISOString() })
  )
  return Promise.all(promises)
}
```

#### 后端中间件（跨域处理）

**文件路径**：`server/middleware.js`

```javascript
module.exports = (req, res, next) => {
  // 允许前端（5173端口）访问后端（3000端口），解决浏览器同源策略限制
  res.header('Access-Control-Allow-Origin', '*')
  res.header('Access-Control-Allow-Methods', 'GET, POST, PUT, DELETE, OPTIONS')
  res.header('Access-Control-Allow-Headers', 'Content-Type, Authorization')
  if (req.method === 'OPTIONS') return res.sendStatus(200)  // 预检请求直接放行
  next()
}
```

---

## 五、次要功能简述

| 功能/文件 | 简述 |
|-----------|------|
| **HomeView.vue** | 首页仪表盘，展示最新5条预约记录，提供快捷导航按钮（预约/刷新/管理） |
| **App.vue** | 根组件，包含顶部全局导航栏（首页/预约/管理）和路由出口 `<router-view>` |
| **router/index.ts** | 定义三条路由：`/`（首页）、`/booking`（预约）、`/admin`（管理） |
| **utils/storage.ts** | 封装 `localStorage` 读写，支持指定默认值，用于 Token 存储 |
| **utils/format.ts** | 日期时间格式化工具函数 |
| **utils/validate.ts** | 通用表单验证规则（手机号、学号格式等） |
| **utils/message.ts** | 封装 Element Plus 消息提示，统一调用方式 |
| **types/\*.ts** | TypeScript 类型定义文件，为各模块数据结构提供类型约束 |
| **components/Base/BaseDialog.vue** | 通用对话框组件（预留扩展） |
| **components/Base/BaseTable.vue** | 通用表格组件（预留扩展） |
| **api/auth.ts** | 认证相关 API（登录/注册，已预留，暂未启用） |
| **db.json** | json-server 数据文件，持久化存储 seats/bookings/violations/notices |

---

## 六、数据流向总结

以**用户提交预约**为例，展示完整数据流：

```
用户点击"提交预约"
        │
        ▼
BookingView.vue（视图层）
  submitBooking()
  ├─ 表单验证（Element Plus）
  ├─ 时间验证（validateBookingTime）
        │
        ▼
useBookingStore.ts（状态层）
  store.addBooking(data)
        │
        ▼
src/api/booking.ts（API 层）
  createBooking(data)
  → axiosInstance.post('/bookings', {...data})
        │
        ▼
json-server（后端服务）
  接收 POST /bookings
  → 写入 db.json（持久化）
  → 返回带 id 的新数据
        │
        ▼
响应返回 → Store 更新本地数组 bookings.push(res)
        │
        ▼
同步调用 store.updateSeat(id, {status: 'occupied'})
→ PATCH /seats/:id → 更新 db.json
        │
        ▼
UI 自动响应（Pinia 响应式）
→ 座位颜色变红
→ 成功弹窗显示
```

---

## 七、答辩常见问题参考

**Q1：为什么选择 json-server 作为后端？**  
A：本项目是前端课程设计，重点在前端实现。`json-server` 只需一个 `db.json` 文件就能模拟完整的 REST API（GET/POST/PATCH/DELETE），启动简单，数据实时持久化，非常适合快速开发和演示。

**Q2：Pinia 和 Vuex 有什么区别，为什么用 Pinia？**  
A：Pinia 是 Vue 3 官方推荐的状态管理库，相比 Vuex 更简洁——没有 mutations，直接修改 state，支持 TypeScript 类型推断更好，也支持 Composition API 写法，与本项目技术栈完全匹配。

**Q3：预约提交和座位状态更新是怎么保持数据一致性的？**  
A：这两步操作是串行的：先等 `addBooking` 的 Promise 完成（写入 db.json 成功），再调用 `updateSeat`。虽然不是严格的数据库事务，但在演示场景下能保证前端数据一致性。

**Q4：表单验证是怎么实现的？**  
A：分两层验证——第一层是 Element Plus 的 `el-form` 内置验证（通过 `:rules` 绑定正则规则），第二层是独立的 `validateBookingTime()` 函数做业务逻辑验证（时长限制等）。只有两层都通过才会提交。

**Q5：跨域问题是怎么解决的？**  
A：通过 `server/middleware.js` 在 json-server 响应头加入 `Access-Control-Allow-Origin: *`，允许任意来源的前端访问，从而解决浏览器的同源策略限制。

---

*文档生成日期：2026-06-16*
